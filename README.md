# helper

一些公用函数
The package of some help function

### 安装说明
```shell
# composer install   
composer config repositories.helper vcs git@github.com:allyfireshane/helper.git
composer require allyfireshane/helper
```